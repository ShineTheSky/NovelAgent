## 长期记忆

长期记忆按归属与用途分为三类，创作正文、大纲、人物和世界观仍必须保存在项目内容目录，不能写入记忆。

| 类型 | 位置 | 何时使用 |
|---|---|---|
| 全局用户偏好 | `global_memory/user.md` | 仅在当前请求相关时由系统注入，跨项目生效。 |
| 项目规则与修正 | `.memory/project_rules.md` | 当前项目的写作、审阅、润色任务按相关性读取。 |
| 参考用途绑定 | `.memory/references/*.md` | 仅当润色或扩写需要借鉴资料时，读取绑定说明并调用 RAG。 |

### 使用规则

- `memory.md` 是系统生成的项目规则索引，不能手工修改。
- 不要创建旧目录 `.memory/user/`、`.memory/feedback/`、`.memory/project/`、`.memory/workflow/` 或 `.memory/reference/`。
- 用户对当前小说的纠正、审阅约束、人物/剧情执行规则，统一补充到 `.memory/project_rules.md`；内容必须长期适用，不能记录一次性任务或已写入大纲、正文的事实。
- 参考绑定须说明“资料来源、适用写作场景、不可照搬的边界”，保存到 `.memory/references/` 中的独立文件；不把整份参考正文复制进记忆。
- 全局用户偏好由后台记忆流程维护。项目 Agent 不得直接写入 `global_memory/`。
- 发生重要错误、明确修正、稳定偏好或关键约束时，调用 `CreateTraceCheckpoint`；Trace 会作为后续记忆提炼的证据，而不是直接作为规则。

## 项目记忆索引

{% if memory_md_content and memory_md_content.strip() %}
{{ memory_md_content }}
{% else %}
暂无项目规则。
{% endif %}
