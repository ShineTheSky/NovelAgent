# NovelAgent2 项目实现逻辑详解

> 面向 Python 初学者的逐层解析，涵盖同步/异步、架构设计、以及"为什么这样做"。

---

## 目录

1. [项目概览](#1-项目概览)
2. [前置概念：同步 vs 异步](#2-前置概念同步-vs-异步)
3. [整体架构：7层分层设计](#3-整体架构7层分层设计)
4. [Layer 1: 数据存储层 (SQLite)](#4-layer-1-数据存储层-sqlite)
5. [Layer 2: LLM 客户端层](#5-layer-2-llm-客户端层)
6. [Layer 3: 上下文组装层](#6-layer-3-上下文组装层)
7. [Layer 4: 工具层](#7-layer-4-工具层)
8. [Layer 5: 安全层](#8-layer-5-安全层)
9. [Layer 6: 记忆系统层](#9-layer-6-记忆系统层)
10. [Layer 7: Agent Loop 核心引擎](#10-layer-7-agent-loop-核心引擎)
11. [子Agent系统](#11-子agent系统)
12. [前端：React + SSE 流式通信](#12-前端react--sse-流式通信)
13. [总结：设计模式与最佳实践](#13-总结设计模式与最佳实践)

---

## 1. 项目概览

NovelAgent2 是一个 **AI 写小说助手**。用户通过网页聊天界面与 AI 对话，AI 可以：

- 读写文件（创建章节、修改大纲、设计角色）
- 搜索文件内容（grep、glob）
- 执行安全的 shell 命令
- 调用子 Agent（章节撰写员、审阅员、角色设计师、大纲规划师）
- 自动记住用户偏好和项目决策

**技术栈**：后端 Python/FastAPI，前端 React/TypeScript/Vite/Tailwind。

---

## 2. 前置概念：同步 vs 异步

### 2.1 什么是"阻塞"？

想象你去奶茶店买奶茶：

- **同步（阻塞）**：你点完单，站在柜台前一直等，不做任何其他事，直到奶茶做好。
- **异步（非阻塞）**：你点完单，拿个号，然后去旁边刷手机。奶茶好了叫号，你再去取。

在程序里：

```python
# 同步方式 —— 会"卡住"
import requests
response = requests.get("https://api.example.com/data")  # 等 2 秒，期间什么都做不了
print(response.text)

# 异步方式 —— 不会"卡住"
import httpx
async def fetch():
    async with httpx.AsyncClient() as client:
        response = await client.get("https://api.example.com/data")  # 发出请求后立即"让路"
        print(response.text)
```

### 2.2 为什么这个项目必须用异步？

这个项目有一个核心场景：**AI 生成文本是流式的**（一个 token 一个 token 地返回），同时前端需要实时显示。

```
用户发消息 → 后端调用 LLM API → LLM 一个字一个字地返回 → 后端把每个字推送给前端
```

如果使用同步方式：
- 整个服务器进程会被 LLM 调用卡住，无法处理其他用户的请求
- 无法在生成过程中中断（用户点了"停止"按钮也没反应）

使用异步方式：
- 等待 LLM 返回时，CPU 可以处理其他事情
- 可以随时响应"停止"信号
- 前端能实时看到每个字逐渐出现（流式体验）

### 2.3 `async` / `await` 快速理解

```python
# async def 定义一个"可以暂停"的函数
async def fetch_data():
    # await 意思是："这里需要等待，我先让出CPU，等好了再继续"
    result = await some_io_operation()
    return result
```

关键规则：
- `await` 只能出现在 `async def` 函数内部
- `await` 后面必须跟一个"可等待对象"（通常是另一个 `async def` 函数的结果）
- `asyncio.create_task()` 可以"后台运行"一个异步任务，不等待它完成

---

## 3. 整体架构：7层分层设计

```
┌─────────────────────────────────────────────┐
│  前端 (React SSE)                            │  ← 用户界面
├─────────────────────────────────────────────┤
│  后端 API (FastAPI + SSE)                    │  ← HTTP 路由
├─────────────────────────────────────────────┤
│  Agent Loop (ReAct 循环引擎)                  │  ← 核心大脑
├──────┬──────┬──────┬──────┬──────┬──────────┤
│Tools │Security│Context│Memory │ LLM │Storage │  ← 6个支撑层
└──────┴──────┴──────┴──────┴──────┴──────────┘
```

每一层只做一件事，层与层之间通过明确的接口通信。这叫做 **关注点分离**。

---

## 4. Layer 1: 数据存储层 (SQLite)

**文件**：`novelagent/storage/database.py`、`models.py`

### 为什么用 SQLite？

| 方案 | 优点 | 缺点 |
|------|------|------|
| SQLite | 零配置、单文件、不需要单独服务 | 并发写入能力弱 |
| PostgreSQL | 高性能、支持并发 | 需要安装和维护 |
| JSON 文件 | 极简 | 查询困难、数据多了很慢 |

对于**桌面级工具**，SQLite 是最优解。不需要用户安装数据库服务，一个文件搞定所有数据。

### 两张表的设计

```sql
projects (project_id, name, genre, word_count, created_at, updated_at)
sessions (session_id, project_id, title, messages_json, token_count, ...)
```

为什么把消息存成 JSON 字符串（`messages_json`）而不是拆成独立的 messages 表？

**是的，这是最优解**。原因：
- 消息列表是一个**整体**，总是整存整取
- 不需要按消息内容查询（不需要 `WHERE content LIKE '%xxx%'`）
- 避免了 N+1 查询问题
- JSON 列在 SQLite 中完全够用

### 连接管理

每次操作都新建连接、用完关闭（`get_connection()` → `conn.close()`）。这不是最高性能的做法（通常会用连接池），但对于 SQLite + 低并发场景，这是正确且简洁的做法。连接池管理会增加复杂度而没有实际收益。

---

## 5. Layer 2: LLM 客户端层

**文件**：`novelagent/llm/client.py`、`config_loader.py`

### 为什么抽象一个 LLMClient？

不同 AI 厂商的 API 格式不同：

| 厂商 | 请求格式 | 响应格式 |
|------|---------|---------|
| Anthropic | `/messages`, `input_schema` | `content_block_delta` |
| OpenAI/DeepSeek/MiniMax | `/chat/completions`, `function` | `choices[].delta` |

LLMClient 做了两件事：
1. **请求转换**：统一的消息格式 → 各厂商要求的格式（`_build_request()`）
2. **响应解析**：各厂商的流式响应 → 统一的 `LLMResponse` 对象

这样上层代码完全不需要关心用的是哪家 AI。

### 配置层 (config_loader.py)

`llm_config.yaml` 中定义了 6 个 **位置 (position)**：

| 位置 | 用途 | 要求 |
|------|------|------|
| `main_loop` | 主对话循环；有对话分支时也承担 Trace 分析 | 最强模型、最大 token 数 |
| `sub_agent` | 子 Agent 执行 | 可按预设覆盖 |
| `memory_prefetch` | 记忆预取 | 低延迟 |
| `memory_summary_fallback` | 无对话分支时的轨迹、记忆与摘要兜底 | 长上下文、稳定输出 |
| `session_title` | 会话标题生成 | 快速、低随机性 |
| `case_analysis` | Agent Bad Case 与 Bash Case 分析 | 长上下文、低随机性 |

这是**成本与职责隔离**的核心策略。记忆预取只判断“哪些文件相关”，强调低延迟；章节撰写需要较强的长文本能力；无可复用对话分支的 Trace 分析、标题生成和案例分析则使用各自独立的位置配置。

`sub_agent` position 还能进一步按 `sub_type` 覆盖，例如 `chapter_writer` 可以使用独立的模型和 token 上限。

### 流式请求 (`_send_request`)

```python
async with client.stream("POST", url, json=body, headers=headers) as response:
    async for line in response.aiter_lines():
        if line.startswith("data: "):
            # 解析每一行 SSE 数据
```

这是 HTTP 流式请求的标准模式。LLM API 返回的是 SSE (Server-Sent Events) 格式，每行一个 `data: {...}`。

### 重试机制

```python
for attempt in range(max_retries + 1):
    try:
        async for chunk in self._send_request(...):
            yield chunk
        return
    except RateLimitError:
        delay = min(2 ** attempt, 30)  # 指数退避: 1s → 2s → 4s → 8s
        await asyncio.sleep(delay)
```

只在速率限制时重试（指数退避），认证错误和服务器错误直接返回。这是合理的设计——重试不应该掩盖真正的配置问题。

**是否为最优解？** 是的。这种多 Provider 适配 + 分位置配置 + 流式解析的设计，是目前 AI 应用的主流做法。

---

## 6. Layer 3: 上下文组装层

**文件**：`novelagent/context/builder.py`、`prompt_manager.py`、`token_counter.py`、`message_manager.py`

### 系统提示词 (System Prompt)

每次 LLM 调用的第一条消息是 system prompt，它告诉 AI "你是谁，你能做什么，你应该怎么做"。

这个项目使用 **Jinja2 模板引擎**来动态生成 system prompt：

```python
# prompt_manager.py
template = self.env.get_template("base_system.j2")
return template.render(project={"name": "...", "genre": "..."}, tools_description="...")
```

为什么用模板？因为 system prompt 需要包含**动态信息**：当前项目名、可用工具列表、记忆索引等。这些信息每次都可能不同，不能写死。

### Token 计数 (token_counter.py)

LLM 的上下文窗口有限（比如 128K tokens）。需要随时知道当前消息占了多少 tokens，快满了就触发压缩。

使用 **tiktoken** 库（OpenAI 开源的 token 计数工具）精确计算。中文大概 1 个字符 ≈ 1-2 个 token。

```python
def needs_compression(self, messages, token_limit, threshold=0.8):
    return self.count_messages(messages) >= token_limit * threshold
```

80% 阈值触发压缩——既不会频繁压缩，也不会等到满了才发现。

### Context 对象

```python
class Context:
    system_prompt: str       # 系统提示词
    messages: list[Message]  # 对话历史
    tool_context: ToolContext # 工具执行上下文

    def to_llm_messages(self) -> list[dict]:
        return [{"role": "system", "content": self.system_prompt}] + [m.to_dict() for m in self.messages]
```

把 system prompt + 消息列表打包成一个对象，统一转换为 LLM 能接收的格式。

**是否为最优解？** 模板引擎 + token 计数是业界标配。有一个小改进空间：目前 token 计数用的是简单的字符串拼接，实际各厂商的 token 计算方式略有差异，但 tiktoken 的估算已经足够准确。

---

## 7. Layer 4: 工具层

**文件**：`novelagent/tools/base.py`、`registry.py`、8 个具体工具

### 工具协议 (ToolProtocol)

所有工具继承同一个抽象基类：

```python
class ToolProtocol(ABC):
    name: str                    # 工具名称
    description: str             # 描述（告诉 LLM 这个工具是干嘛的）
    parameters: dict             # JSON Schema（告诉 LLM 参数格式）

    async def execute(self, params, context) -> ToolResult: ...  # 执行
    def checkPermissions(self, params, context) -> PermissionResult: ...  # 权限判断
```

这就是**面向对象的多态**：上层代码只需要调用 `tool.execute()`，不需要知道具体是哪个工具。

### 工具注册表 (ToolRegistry)

```python
registry = ToolRegistry()
registry.register(ReadTool())
registry.register(WriteTool())
# ...
schemas = registry.get_schemas()  # 生成所有工具的 JSON Schema，传给 LLM
```

LLM 需要知道有哪些工具可用、每个工具接受什么参数。JSON Schema 是 LLM function calling 的标准格式。

### 8 个工具一览

| 工具 | 作用 | 权限 |
|------|------|------|
| Read | 读取文件 | 在工作目录内 → ALLOW |
| Write | 创建/覆盖文件 | 7 步决策链 |
| Edit | 精确字符串替换 | 复用 Write 的权限逻辑 |
| Glob | 文件名模式匹配 | ALLOW |
| Grep | 文件内容搜索 | ALLOW |
| Bash | 执行 Shell 命令 | 3 级分类 |
| SubAgent | 启动子 Agent | ALLOW |
| AskUserQuestion | 向用户提问 | ALLOW（特殊处理） |

### Write 工具的 7 步权限决策

这是一个很好的**安全设计**案例：

```
1. 写入 .memory/ 目录？ → ALLOW（记忆系统免确认）
2. 路径在工作目录外？ → ASK
3. 触及保护目录(.git/.env等)？ → ASK
4. 匹配 allow_rules 配置？ → ALLOW（如 chapters/** 自动放行）
5. acceptEdits 模式开启？ → ALLOW
6. 内部可编辑文件？ → ALLOW
7. 会话内已批准过？ → ALLOW
8. 其他 → ASK
```

从宽松到严格逐层判断，既保证了安全，又不会频繁弹窗打扰用户。

**是否为最优解？** 工具协议的设计是清晰的。一个可优化的点：BashTool 中的命令分类列表（ALLOW/ASK/BLOCKED）在 `BashClassifier` 和 `BashTool` 中**重复了**，应该统一到一处管理。

---

## 8. Layer 5: 安全层

**文件**：`novelagent/security/permission_checker.py`、`path_validator.py`、`bash_classifier.py`

### 三层权限检查

```
1a. 静态规则检查 (_check_static_rules)
     ↓ PASSTHROUGH（不表态）
1b. 风险评估 (_estimate_risk)
     ↓ PASSTHROUGH（不表态）
1c. 工具自主判断 (tool.checkPermissions())
     ↓ 返回最终结果
```

这是一个**责任链模式**。每层可以返回：
- `ALLOW`：直接放行
- `BLOCK`：直接拒绝
- `ASK`：需要用户确认
- `PASSTHROUGH`：不表态，交给下一层

### 路径沙箱 (PathValidator)

核心逻辑只有一行：

```python
if not resolved.startswith(working_dir):
    return False  # 路径逃逸！
```

确保所有文件操作都限制在工作目录内。同时维护了一个系统路径黑名单（`/etc`、`C:\Windows` 等）作为第二道防线。

### Bash 命令分类器 (BashClassifier)

三级分类：

```
ALLOW  → ls, cat, find, grep, wc...（纯读取）
ASK    → sed, awk, rm, mv, git, >重定向（有写操作）
BLOCKED → sudo, pip, npm, kill, nc...（危险操作）
```

未知命令默认归入 ASK（保守策略）。

**是否为最优解？** 三层权限链的设计是合理的。但改进空间：
1. BashClassifier 和 BashTool 中的命令列表重复（前面已提到）
2. 路径检查用 `startswith` 不是严格的安全边界——应该用 `os.path.commonpath()` 来防止符号链接绕过

---

## 9. Layer 6: 记忆与 Trace 系统层

**文件**：`novelagent/memory/memory_manager.py`、`file_store.py`、`index_manager.py`、`prefetcher.py`，以及 `novelagent/trace/`

### 为什么需要记忆系统？

LLM 是无状态的——每次请求是独立的。但写小说需要**长期记忆**：
- 用户偏好（"我喜欢第一人称"）
- 项目决策（"主角的职业改为医生"）
- 用户纠正（"上次写得太严肃了"）

这些信息如果全放在对话历史里，很快会超出上下文窗口。系统将执行过程保存为不可变 Trace，再把分析得到的知识持久化为文件，需要时按层级注入。

### 核心架构

```
MemoryManager (上下文侧总控)
├── FileStore        → 读写 .memory/ 目录下的 Markdown 文件
├── IndexManager     → 生成 memory.md 索引表
└── Prefetcher       → 收到用户消息时，提前查找相关记忆

Trace 分析生命周期
Trace（SQLite 不可变证据）
  → Insight（单次观察或待验证假设）
  → Memory（明确、可复用的原子记忆）
  → Pattern（多条 Memory 支持的稳定模式）
```

旧的 `AutoMemory` 定时扫描路径已经移除。现在由后台 Trace 分析负责自动提取、合并和晋级知识，每条派生记录都保留 `trace_id` 与 `source_event_ids`，可回溯到原始事件。

### 记忆文件格式 (YAML Frontmatter)

```markdown
---
type: user
tags: [偏好, 叙事视角]
summary: 用户偏好第一人称叙事
created: 2026-05-12T10:00:00
updated: 2026-05-12T14:30:00
status: active
---

用户明确表示喜欢第一人称叙事，认为这样更有代入感。
```

这种格式的优点：**人可读** + **程序可解析**。即使用记事本打开也能看懂。

### 预取机制 (Prefetcher)

```python
memory_index, prefetch_task = self.memory.prepare_context(request.content)
# ... 同时构建上下文
memories = await prefetch_task
```

这是一个很好的**异步编程实践**：

1. `prepare_context()` 读取一次 `memory.md` 快照并立即启动相关记忆检索
2. 主流程继续构建上下文、渲染模板
3. 在需要注入附件时再等待预取结果
4. 超时由 `Prefetcher` 按 `memory_prefetch.timeout` 控制，当前配置为 1.5 秒

这样记忆检索不会长期阻塞当前回复。预取只判断“哪些记忆文件相关”，因此使用独立的低延迟 position。

### Trace 派生知识

每轮执行的消息、工具调用、结果与错误先写入 SQLite Trace。后台分析将原始事件整理为带 provenance 的标准化轨迹，再生成 Insight、Memory 或 Pattern。容量淘汰和人工降级只改变派生层级，不删除 Trace 证据。

**当前设计的关键取舍：**
1. Trace 是事实来源，派生文件可以重建、合并或降级
2. Prefetcher 超时后不会阻塞主流程，但当前轮可能缺少相关记忆附件
3. `IndexManager.rebuild()` 是同步文件操作，记忆规模很大时仍可能产生短暂开销

---

## 10. Layer 7: Agent Loop 核心引擎

**文件**：`novelagent/core/agent_loop.py`

这是整个项目的**大脑**。它实现了 **ReAct 模式**（Reasoning + Acting）。

### ReAct 循环流程

```
用户消息
  ↓
1. 启动记忆预取（后台异步）
2. 构建上下文（系统提示词 + 对话历史 + 记忆注入）
3. 等待预取完成（当前配置为1.5秒超时）
4. 超过压缩阈值时，组合 Memory Summary 与尚未覆盖的原始轮次
  ↓
┌─────────────────────────────────┐
│ 5. 调用 LLM（流式）              │
│ 6. LLM 返回：文本 or 工具调用？   │
│    ├── 文本 → 最终回复，结束循环  │
│    └── 工具调用 → 安全检查 → 执行  │
│         ↓                       │
│    7. 工具结果追加到对话历史       │
│    8. 检测循环（连续3次相同→终止） │
│    9. 回到步骤5                   │
└─────────────────────────────────┘
  ↓
10. 保存消息与关联 Trace
11. 后台分析 Trace，派生 Insight / Memory / Pattern
```

主 Agent 当前最多 50 轮循环，防止无限循环；各子 Agent 还可在预设中使用独立上限。

### 为什么用 Generator？

```python
async def run(self, request, session, project_info):
    # ...
    async for chunk in self.llm.chat(...):
        yield ResponseChunk(type="text_delta", data={"delta": chunk.content})
```

`yield` 关键字让这个函数变成一个**流式生成器**。每产生一个 chunk 就立即推送给前端，不需要等整个 LLM 回复完成。

这是 SSE 流式响应的核心机制。前端收到的效果就是 AI 一个字一个字地"打字"出来。

### 权限中断机制

```python
if perm == PermissionResult.ASK:
    session.permission_event = asyncio.Event()  # 创建一个"暂停信号"
    yield ResponseChunk(type="permission_ask", ...)
    await session.permission_event.wait()  # 暂停在这里！
    # 用户点了允许/拒绝后，permission_event.set() 唤醒
    if not session.permission_granted:
        continue  # 跳过这个工具
```

这是**事件驱动**的异步模式。`asyncio.Event()` 像一个"红绿灯"：
- `.wait()` = 红灯，等待
- `.set()` = 绿灯，继续

前端 POST 到 `/permission-response` → 触发 `.set()` → 循环继续。

同样的机制也用在 `AskUserQuestion`——只是换成了 `question_event`。

### 上下文压缩

```python
if token_counter.needs_compression(messages, token_limit, compression_threshold):
    compression_meta = await build_cumulative_compression(
        trace_store,
        session.session_id,
        current_user_message=request.content,
        legacy_messages=session.messages,
    )
    ctx.messages = [Message.from_dict(item) for item in compression_meta["messages"]]
```

当 token 数超过配置阈值（默认 80%）时触发。压缩过程本身不再调用独立的 `context_compression` LLM position，而是组合上一次压缩基线、已经持久化的 Memory Summary，以及尚未被摘要覆盖的原始会话轮次。随后系统创建关联的 continuation Trace，保留压缩前后的来源关系。

这是**关键设计**：它避免重复生成摘要，同时不会因为只有部分轮次已经产生 Memory Summary 而静默丢失未覆盖内容。

### 循环检测

```python
if current_tool_calls == last_tool_calls:
    loop_count += 1
    if loop_count >= 3:
        yield ResponseChunk(type="done", data={"finish_reason": "loop_detected"})
        return
```

防止 LLM 陷入死循环（反复调用同一个工具同样的参数）。

**是否为最优解？** Agent Loop 设计是合理的。一个小问题：压缩逻辑只在进入循环前检查一次，循环过程中如果消息快速增长可能不会触发压缩。更好的做法是每轮都检查。

---

## 11. 子Agent系统

**文件**：`novelagent/core/subagent.py`、`tools/subagent_tool.py`、`config/subagent_presets.yaml`

### 为什么要分子Agent？

主 Agent 处理高层决策（"用户想写什么"），子 Agent 处理具体任务（"写第 3 章"）。

优点：
1. **上下文隔离**：每个子 Agent 有独立的系统提示词，不会污染主 Agent 的上下文
2. **专业化**：章节撰写员的提示词写了 200 多行写作技巧，审阅员有审阅要点
3. **可更换模型**：章节撰写可以用文笔好的模型，审阅可以用逻辑强的模型
4. **流式转发**：子 Agent 的输出实时转发到前端

### 子Agent 预设配置

```yaml
chapter_writer:
  system_prompt: |  # 200+ 行的写作指导
  tools: [Read, Write, Edit, Glob, Grep, Bash]
  inherit_history: false  # 不继承主Agent的对话历史
  max_turns: 10
```

`inherit_history: false` 意味着子 Agent 只看到当前任务，不看到整个对话历史。这样上下文更干净，也省钱。

### 子Agent 和主Agent 共用执行引擎

```python
async def spawn_and_run(self, preset_name, task, parent_session, ...):
    # 构建子Agent的system prompt
    # 调用共享的 query() 函数
    async for ev in query(llm_client, position="sub_agent", ...):
        yield ev  # 转发到前端
```

`llm_turn.py` 中的 `query()` 函数是**主Agent和子Agent共用的执行引擎**——这是一个好的代码复用设计。

**是否为最优解？** 子Agent 预设通过 YAML 配置是灵活的。改进空间：目前子Agent 的结果直接返回文本，如果子Agent 也能返回结构化结果（JSON），主Agent 可以更好地利用子Agent 的输出。

---

## 12. 前端：React + SSE 流式通信

**文件**：`web/src/App.tsx`、`web/src/hooks/useChat.ts`、`web/src/api/client.ts`

### 为什么用 SSE 而不是 WebSocket？

| 特性 | SSE | WebSocket |
|------|-----|-----------|
| 方向 | 单向（服务器→客户端） | 双向 |
| 协议 | HTTP | 独立协议 |
| 复杂度 | 极简 | 需要心跳、重连等 |
| 浏览器支持 | 原生 `EventSource` | 原生 `WebSocket` |

NovelAgent 的通信模式是：**客户端发一个请求 → 服务器持续推送回复**。这是典型的 SSE 场景。单向上行通过普通的 `fetch()`/`POST` 即可。

### SSE 事件类型

```typescript
text_delta      → AI 回复的文字片段（实时打字效果）
thinking        → AI 的思考过程
tool_call       → AI 决定调用工具
tool_result     → 工具执行结果
permission_ask  → 需要用户确认
question_ask    → AI 向用户提问
subagent_done   → 子Agent 完成
done            → 整个回复结束
error           → 错误
```

每种事件类型对应前端的一个渲染组件。

### useChat Hook：状态管理核心

前端不引入 Redux 或其他状态管理库，而是用 React 自带的 `useState` + `useRef` 集中在 `useChat.ts` 这一个 hook 里。

这是正确的选择——**在不需要跨页面共享状态时，不需要全局状态管理**。

### 权限弹窗的异步流程

```
后端 yield permission_ask → 前端显示 PermissionDialog → 用户点击允许/拒绝
  → fetch POST /permission-response → 后端 permission_event.set() → 循环继续
```

这是一个**完整的异步通信链路**。后端通过 SSE 推送问题，前端通过 HTTP POST 回传答案。

---

## 13. 总结：设计模式与最佳实践

### 项目中用到的设计模式

| 模式 | 在哪里 | 解决的问题 |
|------|--------|-----------|
| **分层架构** | 7 层后端 | 职责分离，每层可独立测试和替换 |
| **抽象基类/协议** | ToolProtocol | 统一工具接口 |
| **注册表** | ToolRegistry | 动态管理工具集合 |
| **责任链** | 三层权限检查 | 逐层判断，灵活组合 |
| **模板方法** | Jinja2 渲染 prompt | 动态生成系统提示词 |
| **观察者/事件** | SSE + asyncio.Event | 实时推送和异步等待 |
| **生成器** | async generator (yield) | 流式数据处理 |
| **策略模式** | LLMClient 多 Provider | 可切换 AI 厂商 |

### 对初学者的学习路线建议

1. **先理解同步/异步**：手动写几个 `async`/`await` 的小例子，感受"让出 CPU"的含义
2. **理解 Agent Loop**：这是整个项目的核心，从 `agent_loop.py` 的 `run()` 函数开始追踪
3. **挑一个工具看懂**：比如 `ReadTool`，只有 50 行，理解它如何被注册、调用、安全检查
4. **追踪一次完整请求**：从用户点击"发送" → `useChat.ts` → `fetch` → `FastAPI` → `AgentLoop.run()` → `LLMClient.chat()` → 逐字返回 → 前端渲染
5. **理解记忆系统**：为什么需要记忆、如何存储、如何检索

### 架构上的改进建议（供进阶参考）

1. **命令列表去重**：BashClassifier 和 BashTool 中的 ALLOW/ASK/BLOCKED 列表重复，应抽取到独立模块
2. **压缩时机**：上下文压缩只在进入循环前检查一次，改为每轮检查更安全
3. **预取 fallback**：记忆预取超时后应降级为关键词匹配，而非直接放弃
4. **路径安全**：`startswith` 检查不够严谨，用 `os.path.commonpath()` 防符号链接绕过
5. **子Agent 结构化输出**：子Agent 结果目前是纯文本，改为 JSON 结构化更利于主Agent 下游处理

---

*文档生成日期：2026-05-12*
