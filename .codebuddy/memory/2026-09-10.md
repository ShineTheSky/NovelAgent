# 2026-09-10

## 运行环境核查
- 用户问「哪个 conda 环境可以运行本项目」。逐个环境用 `python -c importlib.util.find_spec` 探测依赖，结论：现有 6 个环境都不完整。

## 新建 conda 环境 novelagent
- 用户选择新建环境：`conda create -n novelagent python=3.12 -y` → Python 3.12.14（D:\soft\miniforge3\envs\novelagent）。
- pip 安装 fastapi / uvicorn[standard] / tiktoken / python-dotenv / pydantic / pydantic-settings / sqlalchemy / aiosqlite / httpx / jinja2 / pyyaml，全部成功，无缺失。
- 冒烟：`import novelagent.server.app` 通过。
- 附带发现：本机 PowerShell PATH 中没有 node / npm，前端 `web/` 暂时跑不起来（需先装 Node ≥ 18）。
- 后续按用户要求：① `conda install -n novelagent -c conda-forge nodejs=22 -y` → nodejs 22.23.2 + npm 10.9.8 已装进 novelagent 环境
  （可执行文件在 D:\soft\miniforge3\envs\novelagent\，激活环境后 node/npm 直接可用）；② 新建 `requirements.txt`（顶层 11 个依赖 + Python>=3.11 注释），
  `pip install --dry-run -r requirements.txt` 校验通过。
- 前端依赖已装：`cd web && npm install` → 266 包，node_modules 落在**项目目录** `web/` 下（不是 conda 环境里）。
- 关键坑：直接调用 `...\envs\novelagent\npm.cmd` 而**未激活环境**时，vite 内部调 `node` 会报
  `'node' is not recognized`。必须先 `conda activate novelagent`（或把 envs\novelagent 与 envs\novelagent\Scripts 加入 PATH）。
  加 PATH 后 `npm run build`（tsc -b && vite build）通过：287 modules，dist 产物 ~450KB。
- `npm install` 复核为 up to date（19 个顶层依赖就位）。`npm ls` 提示 2 个 extraneous（@tybys/wasm-util、tslib），
  无害，可用 `npm prune` 清理。
